# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Entorno virtual
venv/
.venv/
env/
.env

# Variables de entorno (NUNCA subir a Git)
.env

# DVC - el CSV crudo va en remote storage
/data/raw/
!data/raw/.gitkeep
!data/raw/*.dvc

# DVC outputs procesados — se commitean para despliegue portable
# (son pequeños: ~220 KB total). Regenerar con: dvc repro
/data/processed/
!data/processed/.gitkeep
!data/processed/train.parquet
!data/processed/test.parquet
!data/processed/feature_names.json

# Modelo entrenado — se commitea para despliegue sin necesidad de DVC pull
# (sklearn Pipeline ~ pocos MB). Regenerar con: dvc repro train
/models/
!models/.gitkeep
!models/preprocessor.joblib
# MLflow
mlruns/
mlartifacts/

# Reports — se commitean para el dashboard de métricas
reports/
!reports/.gitkeep
!reports/metrics.json
!reports/confusion_matrix.png

# Jupyter
.ipynb_checkpoints/
*.ipynb

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
.docker/

# Coverage
.coverage
htmlcov/
.pytest_cache/
